import { Routes } from '@angular/router';
import { LandingScreenComponent } from './features/unauthorized/landing-screen/landing-screen';

export const routes: Routes = [
  { path: '', component: LandingScreenComponent }
];