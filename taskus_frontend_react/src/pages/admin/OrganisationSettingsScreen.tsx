import { AuthorizedLayout } from '@/components/layout/AuthorizedLayout';

export const OrganisationSettingsScreen = () => {
  return (
    <AuthorizedLayout title="Organisation Settings">
      <p>Organisation settings will go here</p>
    </AuthorizedLayout>
  );
};